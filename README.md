# Tic-Tac-Toe

## Run the game

1. Open PowerShell.
2. Run:

```powershell
cd "C:\Users\droub\Desktop\Personal Projects\tictacreact\ticreactgame"
npm run dev
```

3. Open the `localhost` URL shown in PowerShell.

Press `Ctrl+C` in PowerShell to stop the game.
